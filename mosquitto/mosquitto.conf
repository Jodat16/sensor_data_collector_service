listener 1883

# Matches the environment REQ-2 describes. A broker exposed beyond localhost
# would require authentication -- the service supports it via MQTT_USERNAME /
# MQTT_PASSWORD.
allow_anonymous true

# Writes session state, queued and retained messages to disk so they survive a
# broker restart. 
persistence true
persistence_location /mosquitto/data/
